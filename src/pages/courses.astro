---
import Layout from '../layouts/Layout.astro';
import CountdownTimer from '../components/common/timer/CountdownTimer.astro';
import RegisterButton from '../components/common/buttons/RegisterButton.astro';
import CompanyCarousel from '../components/common/carousel/CompanyCarousel.astro';
import BlogCarousel from '../components/common/carousel/BlogCarousel.astro';

// Fetch homepage data from API
const response = await fetch('https://interview-node.vadakurry.com/wp-json/interview-node/v1/courses-sections');
const apiData = await response.json();
const { data } = apiData;

    const homepageresponse = await fetch('http://interview-node.vadakurry.com/wp-json/interview-node/v1/home-sections');
    const homepageapiData = await homepageresponse.json();
    const homepageData = homepageapiData.data; // or const { data: homepageData } = homepageapiData;

---

<Layout>
     <!-- todo:----------------- (1) Hero Section ----------------- -->
       <section class="bg-gradient-to-r from-[#0B207C] to-[#637CF4] text-white py-20 md:py-32">
         <div class="container mx-auto px-4 lg:px-10 2xl:px-20">
           <div class="flex flex-col lg:flex-row gap-12 items-center">
             <!-- Left Content Area -->
             <div class="lg:w-1/2 space-y-8">
               <div>
                 <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight mb-6">
                   {data.hero.title}
                   <br>
                   <span class="text-white">{data.hero.subtitle}</span>
                 </h1>
               </div>

               <!-- Who is this for? Card -->
               <div class="bg-[#0B207C]/30 rounded-2xl p-8 border border-blue-400/20">
                 <h2 class="text-2xl font-semibold mb-6"> {data.hero.target_audience.title}</h2>

                 <div class="space-y-4">
                    {data.hero.target_audience.points.map((item) => (
                   <div class="flex items-start gap-3">
                     <div class="rounded-full bg-blue-400/20 p-1 mt-1 flex-shrink-0">
                       <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-300" viewBox="0 0 20 20" fill="currentColor">
                         <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                       </svg>
                     </div>
                     <p class="text-lg">{item}</p>
                   </div>
                   ))}

                   <!-- <div class="flex items-start gap-3">
                     <div class="rounded-full bg-blue-400/20 p-1 mt-1 flex-shrink-0">
                       <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-300" viewBox="0 0 20 20" fill="currentColor">
                         <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                       </svg>
                     </div>
                     <p class="text-lg">If you are preparing for a transition from your existing back-end role</p>
                   </div> -->
                 </div>
               </div>
             </div>

             <!-- Right Content Area - Countdown and Register -->
             <div class="lg:w-1/2 flex justify-center">
               <div class="bg-white text-gray-800 rounded-2xl overflow-hidden shadow-xl w-full max-w-md">
                 <div class="p-8">
                   <h2 class="text-2xl font-semibold text-gray-800 mb-6">Next Workshop in</h2>

                   <!-- Countdown Timer - Using dark blue for timer blocks -->
                   <div class="mb-8">
                     <CountdownTimer
                       timestamp={homepageData.hero.workshop.upcoming.timestamp}
                       timerId="ml-program-countdown"
                       bgColor="bg-primary"
                       textColor="text-white"
                       labelColor="text-white"
                     />
                   </div>

                   <!-- Register Button -->
                   <RegisterButton
                   link={homepageData.hero.workshop.upcoming.link}
                     textColour="text-white"
                     backgroundColour="bg-primary"
                     svgBackgroundColour="bg-white"
                     svgTextColour="text-primary"
                   />
                 </div>
               </div>
             </div>
           </div>
         </div>
       </section>

        <!-- todo:----------------- (2) Top firms we help you get hired at ----------------- -->
         <section class="px-4 lg:px-10 2xl:px-20">
            <section class="py-12 md:py-16 container mx-auto">
                <h2 class="text-2xl font-semibold text-center text-primary mb-10"> {data.companies.title}</h2>
                <CompanyCarousel logos={data.companies.logos}/>
            </section>
         </section>
         <!-- todo:----------------- (3) ML Curiculums ----------------- -->
         <section class="px-4 lg:px-10 2xl:px-20 py-16 md:py-24 bg-[#E5EAFF]/50">
           <div class="container mx-auto">
             <!-- Section title -->
             <h2 class="text-3xl md:text-4xl font-bold text-primary text-center mb-12">
               {data.curriculum.title}
             </h2>

             <!-- Accordion container -->
             <div class="max-w-4xl mx-auto space-y-4 pb-60">
               <!-- Module 1 (expanded by default) -->
               <!-- <div class="accordion-item border border-gray-200 rounded-lg overflow-hidden">
                 <button class="accordion-header w-full bg-white text-left px-6 py-4 flex justify-between items-center" aria-expanded="true">
                   <span class="text-primary font-semibold">Python Programming Proficiency</span>
                   <div class="bg-[#0B207C1A] border-[#0B207C1A] border rounded-xl p-2">

                       <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-primary transform rotate-180 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                    </svg>
                </div>

                 </button>
                 <div class="accordion-content bg-white p-6" aria-hidden="false">
                   <ul class="space-y-4 list-inside text-gray-700">
                     <li class="flex items-start">
                       <span class="text-primary font-bold mr-2">•</span>
                       <span>Python Programming Proficiency</span>
                     </li>
                     <li class="flex items-start">
                       <span class="text-primary font-bold mr-2">•</span>
                       <span>Data Structures and Algorithms</span>
                     </li>
                     <li class="flex items-start">
                       <span class="text-primary font-bold mr-2">•</span>
                       <span>Project: Implement key algorithms and data structures in Python, focusing on efficiency and performance.</span>
                     </li>
                   </ul>
                 </div>
               </div> -->

               <!-- Module 2 -->
               {data.curriculum.sections.map((section) => (
               <div class="accordion-item border border-gray-200 rounded-lg overflow-hidden">
                 <button class="accordion-header w-full bg-white text-left px-6 py-4 flex justify-between items-center">
                   <span class="text-primary font-semibold">{section.collapse_title}</span>
                   <div class="bg-[#0B207C1A] border-[#0B207C1A] border rounded-xl p-2">
                       <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-primary transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                    </svg>
                </div>
                 </button>
                 <div class="accordion-content bg-white p-6 hidden">
                   <ul class="space-y-4 list-inside text-gray-700">
                     <li class="flex items-start">
                       <span class="text-primary font-bold mr-2">•</span>
                       <span>{section.points[0]}</span>
                     </li>
                     <li class="flex items-start">
                       <span class="text-primary font-bold mr-2">•</span>
                       <span>{section.points[1]}</span>
                     </li>
                   </ul>
                 </div>
               </div>
               ))}


             </div>


           </div>


           <script is:inline>
             document.addEventListener('DOMContentLoaded', function() {
               // Select all accordion headers
               const accordionHeaders = document.querySelectorAll('.accordion-header');

               // Add click event listeners to all headers
               accordionHeaders.forEach(header => {
                 header.addEventListener('click', function() {

                   // Toggle the aria-expanded attribute
                   const expanded = this.getAttribute('aria-expanded') === 'true' || false;
                   this.setAttribute('aria-expanded', !expanded);

                   // Get the content panel
                   const content = this.nextElementSibling;

                   // Toggle the hidden class and aria-hidden attribute
                   if (expanded) {
                     content.classList.add('hidden');
                     content.setAttribute('aria-hidden', 'true');
                     this.querySelector('svg').classList.remove('rotate-180');
                     header.classList.add('bg-white');
                     header.classList.remove('bg-gray-100');
                   } else {
                     content.classList.remove('hidden');
                     content.setAttribute('aria-hidden', 'false');
                     this.querySelector('svg').classList.add('rotate-180');
                     header.classList.remove('bg-white');
                     header.classList.add('bg-gray-100');
                   }
                 });
               });
             });
           </script>
         </section>
         <!-- todo:----------------- (4) CTA Section ----------------- -->
         <div class="mx-auto -mt-60 bg-gradient-to-r from-[#0B207C] to-[#637CF4] text-white rounded-3xl p-8 w-[90%] md:p-12 lg:w-[70%] 2xl:w-[60rem] mb-10">
            <div class="max-w-3xl mx-auto text-center">
              <h3 class="text-2xl md:text-3xl font-bold mb-4"> {data.features.title}</h3>
              <p class="text-white/80 mb-8">
                {data.features.subtitle}
              </p>

              <div class="mb-8">
                <h4 class="text-xl font-semibold mb-4">Next Workshop in</h4>
                <div class="flex justify-center">
                  <CountdownTimer
                    timestamp={homepageData.hero.workshop.upcoming.timestamp}
                    timerId="curriculum-countdown-2"
                    bgColor="bg-white"
                    textColor="text-primary"
                    labelColor="text-gray-500"
                  />
                </div>
              </div>

              <div class="flex justify-center">
                <RegisterButton
                  link={homepageData.hero.workshop.upcoming.link}
                  textColour="text-primary"
                  backgroundColour="bg-white"
                  svgBackgroundColour="bg-primary"
                  svgTextColour="text-white"
                />
              </div>
            </div>
          </div>
          <!-- todo:----------------- (5) why interview node ----------------- -->
          <section class="px-4 lg:px-10 2xl:px-20 py-16 md:py-24">
            <div class="container mx-auto">
              <!-- Section title -->
              <h2 class="text-3xl md:text-4xl font-bold text-primary text-center mb-16">
                {data.why_us.title}
              </h2>

              <!-- Features grid -->
              <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                {data.why_us.features.map((feature: any) => (
                <div class="bg-white rounded-xl p-8 shadow-sm border border-gray-100">
                  <div class="inline-flex items-center justify-center w-16 h-16 bg-white rounded-lg shadow-sm border border-gray-100 mb-6">
                   <img src={feature.icon_url} alt={feature.title} class="size-9">
                  </div>

                  <h3 class="text-xl font-semibold text-gray-900 mb-2">
                    {feature.title}
                  </h3>

                  <p class="text-gray-700">
                    {feature.description}
                  </p>
                </div>
                ))}
              </div>

            </div>
          </section>
              <!-- todo:----------------- (6) Fraquently asked quistions ----------------- -->
         <section class="px-4 lg:px-10 2xl:px-20 py-16 md:py-24 bg-[#E5EAFF]/50">
            <div class="container mx-auto">
              <!-- Section title -->
              <h2 class="text-3xl md:text-4xl font-bold text-primary text-center mb-12">
                Frequently Asked Questions
              </h2>

              <!-- Accordion container -->
              <div class="max-w-4xl mx-auto space-y-4 pb-60">
                <!-- Module 1 (expanded by default) -->
                {/* <div class="accordion-item border border-gray-200 rounded-lg overflow-hidden">
                  <button class="accordion-header w-full bg-white text-left px-6 py-4 flex justify-between items-center" aria-expanded="true">
                    <span class="text-primary font-semibold">Python Programming Proficiency</span>
                    <div class="bg-[#0B207C1A] border-[#0B207C1A] border rounded-xl p-2">

                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-primary transform rotate-180 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                         <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                     </svg>
                 </div>

                  </button>
                  <div class="accordion-content bg-white p-6" aria-hidden="false">
                    <ul class="space-y-4 list-inside text-gray-700">
                      <li class="flex items-start">
                        <span class="text-primary font-bold mr-2">•</span>
                        <span>Python Programming Proficiency</span>
                      </li>
                      <li class="flex items-start">
                        <span class="text-primary font-bold mr-2">•</span>
                        <span>Data Structures and Algorithms</span>
                      </li>
                      <li class="flex items-start">
                        <span class="text-primary font-bold mr-2">•</span>
                        <span>Project: Implement key algorithms and data structures in Python, focusing on efficiency and performance.</span>
                      </li>
                    </ul>
                  </div>
                </div> */}

                {data.faq.sections.map((question) => (
                <div class="accordion-item border border-gray-200 rounded-lg overflow-hidden">
                  <button class="accordion-header w-full bg-white text-left px-6 py-4 flex justify-between items-center">
                    <span class="text-primary font-semibold">{question.question}</span>
                    <div class="bg-[#0B207C1A] border-[#0B207C1A] border rounded-xl p-2">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-primary transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                         <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                     </svg>
                 </div>
                  </button>
                  <div class="accordion-content bg-white p-6 hidden">
                    <ul class="space-y-4 list-inside text-gray-700">
                    {question.points.map((answer) => (
                      <li class="flex items-start">
                        <span class="text-primary font-bold mr-2">•</span>
                        <span>{answer}</span>
                      </li>
                    ))}

                    </ul>
                  </div>
                </div>
                ))}


              </div>


            </div>

          </section>
          <!-- todo:----------------- (7) CTA Section ----------------- -->
          <div class="mx-auto -mt-60 bg-gradient-to-r from-[#0B207C] to-[#637CF4] text-white rounded-3xl p-8 w-[90%] md:p-12 lg:w-[70%] 2xl:w-[60rem] mb-10">
             <div class="max-w-3xl mx-auto text-center">
               <h3 class="text-2xl md:text-3xl font-bold mb-4"> {data.features.title}</h3>
               <p class="text-white/80 mb-8">
                 {data.features.subtitle}
               </p>

               <div class="mb-8">
                 <h4 class="text-xl font-semibold mb-4">Next Workshop in</h4>
                 <div class="flex justify-center">
                   <CountdownTimer
                     timestamp={homepageData.hero.workshop.upcoming.timestamp}
                     timerId="curriculum-countdown"
                     bgColor="bg-white"
                     textColor="text-primary"
                     labelColor="text-gray-500"
                   />
                 </div>
               </div>

               <div class="flex justify-center">
                 <RegisterButton
                   link={homepageData.hero.workshop.upcoming.link}
                   textColour="text-primary"
                   backgroundColour="bg-white"
                   svgBackgroundColour="bg-primary"
                   svgTextColour="text-white"
                 />
               </div>
             </div>
           </div>
            <!-- todo: (8) blogs -->
        <section class="px-4 lg:px-10 2xl:px-20 py-16 md:py-24 ">
            <section class="container mx-auto">
              <!-- Section title -->
              <h2 class="text-3xl md:text-4xl font-bold text-primary text-center mb-6">
                {data.insights.title}
              </h2>

              <p class="text-center text-gray-700 max-w-4xl mx-auto mb-16">
                {data.insights.description}
              </p>
              <!-- Blog posts carousel -->
              <BlogCarousel blogs={data.insights.items} />
            </section>


          </section>



</Layout>
